# satpen-off -- the OTHER HALF of registered arm 11.
#
# Arm 11 is "CLAMP/satpen interaction (400 vs 600, byte-free)".  Run 61
# answered the CLAMP half (400 loses: 0.01380 vs 0.01362) and closed it, but
# satpen has never been varied in this campaign -- 0.03 has been carried since
# the kbbil lesson made it DEFAULT ON, and defaults that old deserve one
# measurement before the family builds anything else on them.
#
# NAMED QUESTION: does the saturation penalty still earn its place under the
# exported-fidelity rule?  Its job is to keep the residual off the clip; the
# grid-era ml2 runs report clip-saturated 0.00-0.52%, i.e. the penalty may
# already be pushing against a door that is barely closed.  Byte-free either
# way, so this is pure val.
#
# On the l1 .001 operating point, one field from 74, split pinned.
name: satpen_off
data: {source: ../cache_repl8M.pkl, kind: legacy-pkl, split: legacy-perm, valn: 4027406, perm_seed: 0}
model: {arch: ml2, N: 4, base: mat, ternary: 0.85, bm: 4, u2grid: 1, gridste: 1}
loss: {l1: 0.001, satpen: 0.0, satthresh: 480.0}
opt: {epochs: 30, batch: 8192, lr: 3.0e-3, seed: 0}
notes: "registered arm 11's satpen half: is the saturation penalty still
  earning its place under fidelity? one field from 74 (val 0.01347)"
